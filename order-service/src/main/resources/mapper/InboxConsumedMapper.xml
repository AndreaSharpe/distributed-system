<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.orderservice.mapper.InboxConsumedMapper">
    <insert id="insert">
        INSERT INTO inbox_consumed (consumer_group, event_id, consumed_at)
        VALUES (#{consumerGroup}, #{eventId}, NOW())
    </insert>
</mapper>

