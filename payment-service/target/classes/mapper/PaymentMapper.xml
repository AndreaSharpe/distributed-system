<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.paymentservice.mapper.PaymentMapper">
    <select id="findByPaymentNo" resultType="com.example.paymentservice.entity.Payment">
        SELECT * FROM payment WHERE payment_no = #{paymentNo}
    </select>

    <insert id="insert" parameterType="com.example.paymentservice.entity.Payment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO payment (payment_no, order_no, user_id, amount, status, channel, created_at, updated_at)
        VALUES (#{paymentNo}, #{orderNo}, #{userId}, #{amount}, #{status}, #{channel}, NOW(), NOW())
    </insert>

    <update id="updateStatus">
        UPDATE payment
        SET status = #{toStatus}, updated_at = NOW()
        WHERE payment_no = #{paymentNo} AND status = #{fromStatus}
    </update>

    <update id="updateStatusAny">
        UPDATE payment
        SET status = #{toStatus}, updated_at = NOW()
        WHERE payment_no = #{paymentNo}
    </update>
</mapper>

