<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.userservice.mapper.UserMapper">
    <select id="findByUsername" resultType="com.example.userservice.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>
    <select id="findById" resultType="com.example.userservice.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <insert id="insertUser" parameterType="com.example.userservice.entity.User">
        INSERT INTO user (username, password_hash, salt, created_at, updated_at, status)
        VALUES (#{username}, #{passwordHash}, #{salt}, NOW(), NOW(), 1)
    </insert>
</mapper>
